import 'package:flutter/material.dart';

class RecordTextItem extends StatelessWidget {
  const RecordTextItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
